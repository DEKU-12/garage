<!-- Week 1. THE STRICTEST OUTPUT CONTRACT IN THE SYSTEM (TAD §3.4).
     In:  issue + context pack + prior failure feedback (git stderr verbatim)
     Out: a unified diff, fenced, and NOTHING ELSE. Prose is forbidden.
     Must show one worked `diff --git` example. -->
