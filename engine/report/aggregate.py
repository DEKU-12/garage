"""Run logs -> the M1..M6 tables (FR-31).

Build week: 2. THE THIRD LAW: every number in README/resume/reports comes out
of this module over real run logs. Never typed by hand, never estimated,
never "approximately" (rules.md §0.3).

Pure function over files -- trivially rerunnable, which is what makes NFR-1
(every reported number regenerable by one command) true.
"""
