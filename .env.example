# Copy to .env -- never commit the real file (rules.md §1).
# Loaded with load_dotenv(override=True). Keys must never appear in code,
# configs, events, artifacts, or logs.

# Anthropic (claude-sonnet-5, claude-opus-5) -- console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# Groq (openai/gpt-oss-20b, -120b) -- kept for the E4 model bake-off
GROQ_API_KEY=
